/**
 * Versión que se muestra en la barra lateral y en créditos.
 *
 * Se lleva a mano, sin ceremonia, porque el proyecto no publica paquetes:
 * el número sube cuando cambia lo que la aplicación hace, no en cada commit.
 *
 *   v0.1  Prototipo con datos de ejemplo.
 *   v0.2  Curvas de fábrica digitalizadas, modelo físico por leyes de
 *         afinidad, evaluación contra curva, límites operacionales por
 *         unidad y asistente de diagnóstico.
 *   v0.3  Documentación: fichas técnicas con su curva, instructivos de la
 *         intranet, icono propio y ficha de autor al día.
 */
export const VERSION = "v0.3";
